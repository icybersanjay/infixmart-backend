// SEO metadata is handled by Next.js page-level `metadata` / `generateMetadata` exports.
// This component is kept as a no-op so legacy page imports don't break.
const SEO = () => null;

export default SEO;
