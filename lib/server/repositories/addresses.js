// JS re-export shim — see addresses.ts for the real implementation.
export {
  clearDefaultAddresses,
  createAddress,
  deleteAddressForUser,
  findAddressByIdForUser,
  listAddressesByUserId,
  updateAddressForUser,
} from "./addresses.ts";
